
import ProjectCarousel from '../../components/Carousel/Carousel';
const ProjectPage = () => {
  

  return (
    <section>
      <ProjectCarousel />
    </section>
  );
};

export default ProjectPage;
